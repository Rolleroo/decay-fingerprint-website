# Runtime dependencies for the deployed app (Hugging Face Spaces / Streamlit
# Community Cloud install from this file). Pinned for reproducible builds.
# Test-only tools (pytest, mpmath) live in requirements-dev.txt.
radioactivedecay==0.6.1
streamlit==1.58.0
numpy==2.5.0
scipy==1.18.0
pandas==3.0.3
openpyxl==3.1.5
defusedxml==0.7.1  # hardens openpyxl XML parsing (XXE / billion-laughs) on untrusted .xlsx
